# /docs

Drop .md, .txt, .ts, or .js files here to ingest into the RAG system.

The ingestion script (`npm run ingest`) will chunk, embed, and index everything in this folder.

For Week 1 — start with something small and real: a README, a config file, a module you know well.
